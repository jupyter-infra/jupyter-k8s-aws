apiVersion: workspace.jupyter.org/v1alpha1
kind: Workspace
metadata:
  name: ws-bearer-private
  namespace: default
spec:
  displayName: "Private Workspace with Bearer Auth"
  image: uv
  desiredStatus: Running
  accessType: OwnerOnly
  ownershipType: OwnerOnly
  accessStrategy:
    name: bearer-access-strategy
    namespace: default
  resources:
    limits:
      cpu: "1"
      memory: "2Gi"
    requests:
      cpu: "0.5"
      memory: "1Gi"
